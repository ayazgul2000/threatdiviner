services:
  postgres:
    image: postgres:16-alpine
    container_name: td-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: threatdiviner_dev
      POSTGRES_DB: threatdiviner
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d threatdiviner"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    image: redis:7-alpine
    container_name: td-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: redis-server --appendonly yes

  minio:
    image: minio/minio:latest
    container_name: td-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: threatdiviner
      MINIO_ROOT_PASSWORD: threatdiviner_dev
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: server /data --console-address ":9001"

  qdrant:
    image: qdrant/qdrant:latest
    container_name: td-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:6333/readyz"]
      interval: 10s
      timeout: 5s
      retries: 5

  threagile:
    image: threagile/threagile:latest
    container_name: td-threagile
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - threagile_data:/app/data
    environment:
      - THREAGILE_SERVER_PORT=8080
      - THREAGILE_SERVER_MODE=server
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

volumes:
  postgres_data:
    name: td-postgres-data
  redis_data:
    name: td-redis-data
  minio_data:
    name: td-minio-data
  qdrant_data:
    name: td-qdrant-data
  threagile_data:
    name: td-threagile-data

networks:
  default:
    name: threatdiviner-network
