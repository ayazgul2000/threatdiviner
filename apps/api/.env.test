# Test Database
DATABASE_URL="postgresql://threatdiviner:threatdiviner@localhost:5434/threatdiviner_test"

# Redis
REDIS_HOST="localhost"
REDIS_PORT=6380

# JWT
JWT_SECRET="test-jwt-secret-for-testing-only"
JWT_REFRESH_SECRET="test-refresh-secret-for-testing-only"
JWT_EXPIRATION="15m"
JWT_REFRESH_EXPIRATION="7d"

# Server
PORT=3002
CORS_ORIGIN="http://localhost:3000"
NODE_ENV="test"

# Email (MailHog)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noreply@threatdiviner.test

# Webhooks (MockServer)
SLACK_WEBHOOK_URL=http://localhost:1080/api/slack/webhook

# Scanning (disabled for tests)
SCAN_WORKDIR="C:/tmp/threatdiviner-test-scans"
ENABLE_DOCKER_SCANNERS=false

# Token encryption
TOKEN_ENCRYPTION_KEY="test-encryption-key-for-testing"
